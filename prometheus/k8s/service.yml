apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  labels:
    app: maker-monitor
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 9090
      protocol: TCP
  selector:
    service: prometheus